/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "fakenewsapi-1-w3888100.deta.app",
      "localhost",
    ],
  },
};

module.exports = nextConfig;
